class Solution {
    fun solution(x: Int): Boolean {
        var answer = false
        var num = x
        var sum = 0
        
        while(num!=0){
            sum=sum+num%10
            num=num/10
        }
        
        if(x%sum==0){
            answer=true
        }
        
        return answer
    }
}
