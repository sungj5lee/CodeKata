class Solution {
    fun solution(n: Int): String {
        var answer = buildString{
            repeat(n){
                if(it%2==0) append('수') else append('박')
            }
        }
        return answer
    }
}
