class Solution {
    fun solution(arr: IntArray): IntArray {
        var answer = arr.filter{it!=arr.minOf{it}}.toIntArray()
        
        if(answer.isEmpty()){
            answer=answer+(-1)
        }
        
        return answer
    }
}
