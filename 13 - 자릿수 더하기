class Solution {
    fun solution(n: Int): Int {
        var answer = 0
        var num=n
        
        while(num!=0){
            answer = answer + num%10
            num=num/10
        }

        // [실행] 버튼을 누르면 출력 값을 볼 수 있습니다.
        println("Hello Kotlin")

        return answer
    }
}
