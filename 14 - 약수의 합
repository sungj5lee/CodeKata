class Solution {
    fun solution(n: Int): Int {
        var answer = 0
        var prime = 1
        
        while(2*prime<=n){
            if(n%prime==0){
                answer = answer + prime
            }
            prime++
        }
        answer=answer+n
        
        return answer
    }
}
